name: Security

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 1 * *"

permissions:
  contents: read

jobs:
  semgrep:
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep:1.168.0
    steps:
      - uses: actions/checkout@v4
      - run: semgrep --config p/security-audit --config p/owasp-top-ten --exclude '**/*.test.*' --error --metrics=off --jobs 1 src
